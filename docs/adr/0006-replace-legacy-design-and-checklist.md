# The design layer replaces the legacy design and checklist

<a id="adr-0006"></a>

The root `DESIGN.md` mixed intent, implementation examples, and external background, while `TODO.md` mixed completed history with open work. Their current contracts move to the design layer, unresolved work moves to the local tracker, and the source-to-destination map records the migration before the legacy files are removed.
